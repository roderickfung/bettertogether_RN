import {
  ADD_MESSAGE,
  SEND_MESSAGE,
  FETCH_MESSAGES,
  RECEIVED_MESSAGES,
  GENERATE_ROOM,
  DELETE_CHAT
} from './types';
import firebase from 'firebase';

export const findRoom = ({ me, friend}) => {

}
